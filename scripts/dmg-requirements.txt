# Build-time only; none of these packages are shipped inside the application.
# 1.6.7 omits the obsolete pBBk background bookmark, which makes modern
# Finder ignore the otherwise valid backgroundImageAlias and render white.
dmgbuild==1.6.7
ds-store==1.3.3
mac-alias==2.2.3
